import { Command } from 'commander';
export { createValidateCommand } from './validate.js';

/**
 * New Server Command
 *
 * Scaffolds a new MCP server project from a template.
 */

/**
 * Create the 'new' command with 'server' subcommand
 */
declare function createNewCommand(): Command;

/**
 * Template Commands
 *
 * Commands for managing and discovering templates.
 */

/**
 * Create the main 'template' command
 */
declare function createTemplateCommand(): Command;

/**
 * Add Tool Command
 *
 * Scaffolds a new MCP tool with:
 * - Tool file with TODO markers
 * - Unit test file structure
 * - Integration test YAML
 * - Auto-registration in src/index.ts
 */

/**
 * Create the 'add' command group
 */
declare function createAddCommand(): Command;

/**
 * List Command
 *
 * Parent command for listing various project elements.
 */

/**
 * Create list command
 */
declare function createListCommand(): Command;

export { createAddCommand, createListCommand, createNewCommand, createTemplateCommand };
